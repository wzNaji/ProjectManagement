spring.application.name=ProjectManagement

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Eksempel: spring.datasource.username=root
spring.datasource.username=${LOCAL_DATABASE_USERNAME}

#Eksempel spring.datasource.password=password
spring.datasource.password=${LOCAL_DATABASE_PASSWORD}

# Eksempel: spring.datasource.url=jdbc:mysql://localhost:3306/ProjectManagement
spring.datasource.url=${LOCAL_DATABASE_URL}

# Session udløber 30m efter brugerens sidste request
server.servlet.session.timeout=30m


